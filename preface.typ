= はじめに

このテンプレートは、Typst で日本語の文書を書くための最小構成です。
本文は明朝体、見出しはゴシック体を使い、段落の先頭は全角一文字ぶん字下げされます。

体裁の設定は `lib.typ` に集約し、章は `chapters/` 以下のファイルへ分けています。
以降の各章は、数式・コード・引用・脚注といった要素ごとに、
素の Typst では何が起きるか、テンプレートで何を補ったかを示します。
