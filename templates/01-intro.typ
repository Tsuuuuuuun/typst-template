= 段落と章

== 段落と禁則処理

句読点や括弧の禁則処理は Typst が自動で行います。
「こうした鉤括弧」や（丸括弧）も、行頭・行末に来ないよう調整されます。

== 章の分割

章ごとに `chapters/` 以下のファイルへ分け、`main.typ` から `#include` で読み込んでいます。
`#include` は書式設定を持ち込まないので、体裁は `lib.typ` の設定がそのまま効きます。

#pagebreak()
